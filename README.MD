### Wygryzanko - fun with POC

## Getting started

 - Build and start docker
   -  `docker-compose up --build dev`
 - Attach to the container
   - `docker exec -it wygryzanko_dev_1 zsh`
   - **name can be different** - look it up with `docker ps`   
